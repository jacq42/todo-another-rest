apiVersion: v1
kind: Service
metadata:
  name: sonarqube-service
  namespace: devops-tools
spec:
  selector:
    app: sonarqube
  type: NodePort
  ports:
    - port: 9000
      targetPort: 9000
      nodePort: 30900